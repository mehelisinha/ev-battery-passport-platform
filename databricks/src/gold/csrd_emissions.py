"""CSRD Scope 3 emissions per battery lifecycle stage -> Gold.

Roadmap Step 4.4 | PRD Section 3.1 (CSRD), Section 4.4.

Computes carbon footprint per lifecycle stage by joining charging events to the
grid carbon-intensity at each event's timestamp (the "join-on-time" pattern).
This is the metric that unifies the battery and grid domains into one
sustainability number.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("Roadmap Step 4.4 — compute CSRD Scope 3 emissions")


if __name__ == "__main__":
    main()
